---
tags: [FUNCTION, OBSERVABLE, REACTIVE FUNCTION]
---

# reactiveGreaterThan

Alternatives: `greaterThan$$`, `gt$$`

### Types

```ts
function reactiveGreaterThan(
  a: IObservable<any>,
  b: IObservable<any>,
): IObservable<boolean>
```

### Definition

Creates an Observable which performs a greater than comparison (`>`) between the values sent by two Observables, and emits the result.

### Diagram

```mermaid
sequenceDiagram

participant A as observableA
participant B as observableB
participant reactiveGreaterThan as reactiveGreaterThan(observableA, observableB)
participant OUT

A->>reactiveGreaterThan: 1
B->>reactiveGreaterThan: 2
reactiveGreaterThan->>OUT: false
B->>reactiveGreaterThan: 0
reactiveGreaterThan->>OUT: true
```

### Example

#### Perform a greater than comparison of two Observables

```ts
const subscribe = reactiveGreaterThan(
  single(2),
  single(1),
);

subscribe((value: boolean) => {
  console.log(value);
});
```

Output:

```text
true
```
