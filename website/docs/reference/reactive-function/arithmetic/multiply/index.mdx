# 🄾 reactiveMultiply

Alternatives: `mul$$`, `multiply$$`

### Types

```ts
function reactiveMultiply(
  a: IObservable<number>,
  b: IObservable<number>,
): IObservable<number>
```

### Definition

Creates an Observable which multiplies and sends the values emitted by two Observables.

### Diagram

[//]: # (TODO)

### Perform the "multiplication" of two Observables

####

```ts
const subscribe = reactiveMultiply(
  single(10),
  single(5),
);

subscribe((value: number) => {
  console.log(value);
});
```

Output:

```text
50
```
