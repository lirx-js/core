---
tags: [FUNCTION, OBSERVABLE]
---

# empty

### Types

```ts
function empty<GValue = any>(): IObservable<GValue>
```

### Definition

Creates an Observable, which never emits a value.

### Example

```ts
const subscribe = empty();

empty(() => {
  console.log('never appends');
});
```
